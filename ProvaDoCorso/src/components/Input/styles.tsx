import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    backgroundColor: 'light-gray', 
    padding: 15,
    borderRadius: 12,
    color: "black", 
    fontSize: 16,
    width: 300
}
});
